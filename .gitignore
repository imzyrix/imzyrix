__pycache__/
*.pyc
.venv/

# reference clone of the original repo (kept locally, never published)
_ref_original/
# intermediate prepped image (derived) — regenerate with prep_photo/prep_solid_bg
source-prepped.png
# local Discord user ID (secrets go in the repo's DISCORD_USER_ID secret)
scripts/config.json
# intermediate Discord avatar/decoration rasterized assets (built each run)
data/discord/
